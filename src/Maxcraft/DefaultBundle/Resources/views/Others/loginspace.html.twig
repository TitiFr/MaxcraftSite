{# src Maxcraft/DefaultBundle/Resources/views/loginspace.html.twig #}

{% block body %}

    {# S'il y a une erreur, on l'affiche dans un joli cadre #}
    {% if error %}
        <div class="alert alert-danger">{{ error.message }}</div>
    {% endif %}

    {# Le formulaire, avec URL de soumission vers la route « login_check » comme on l'a vu #}
    <p>
        <br>
        <h1 style="">Se connecter</h1>
        <br><br>
    </p>
    <form action="{{ path('login_check') }}" method="post">
        <label for="username">Nom du compte :</label>
        <input type="text" id="username" name="_username" value="{{ last_username }}" />

        <br><br>

        <label for="password">Mot de passe :</label>
        <input type="password" id="password" name="_password" />
        <br /><br>
        <input type="submit" value="Connexion" class="button-bevel"/>
    </form>

{% endblock %}

{# ATTENTION faire un render controller quand inséré dans vue#}